import type { Agent } from "@dfinity/agent";
import type { Principal } from "@dfinity/principal";
type ModuleType = typeof import("@dfinity/nns-proto");
export declare const importNnsProto: () => Promise<ModuleType>;
/**
 * Submits an update call to the IC.
 * @returns The (binary) response if the request succeeded, an error otherwise.
 */
export declare const updateCall: ({ agent, canisterId, methodName, arg, }: {
    agent: Agent;
    canisterId: Principal;
    methodName: string;
    arg: ArrayBuffer;
}) => Promise<Uint8Array>;
/**
 * Submits a query call to the IC.
 * @returns The (binary) response if the request succeeded, an error otherwise.
 */
export declare const queryCall: ({ agent, canisterId, methodName, arg, }: {
    agent: Agent;
    canisterId: Principal;
    methodName: string;
    arg: ArrayBuffer;
}) => Promise<Uint8Array>;
export {};
